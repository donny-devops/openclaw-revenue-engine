interface:
  display_name: "Openclaw Revenue Engine"
  short_description: "Repo-specific patterns and workflows for openclaw-revenue-engine"
  default_prompt: "Use the openclaw-revenue-engine repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true